import { useState, useEffect } from "react";

const useFetch = (callInput, text = null, page = null) => {
  const [images, setImages] = useState([]);

  

  return images;
};

export default useFetch;
